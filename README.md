# Segno Library must be installed on computer
This code is generated using the IDLE IDE that is built-in with Python install.
Due to this, any IDLE version using Python3 should work with the python code.

I have not tested this code with any other IDEs for Python, so I do not know if the code operates correctly.
